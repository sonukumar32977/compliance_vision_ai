{
  "name": "compliance-vision-ppe",
  "private": true,
  "description": "Compliance Vision AI — run npm scripts from project root",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "render-build": "npm run render-build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "test:smtp": "npm run test:smtp --prefix frontend",
    "users:list": "npm run users:list --prefix frontend",
    "users:admin": "npm run users:admin --prefix frontend",
    "prisma:generate": "npm exec --prefix frontend prisma generate",
    "prisma:migrate": "npm exec --prefix frontend prisma migrate deploy",
    "prisma:push": "npm exec --prefix frontend prisma db push"
  }
}
