You are an expert senior full-stack engineer.
Build a complete production-grade web application called Compliance Vision AI — a real-time 
PPE violation detection and compliance reporting platform.
Tech stack:
- Frontend: Next.js 14 (App Router), React, TypeScript, Tailwind CSS, 
shadcn
/
ui
, 
  
lucide
-react, Framer Motion, Recharts
- Backend: Next.js API Routes (under /app/
api
/)
- Database: MySQL via Prisma ORM
- Auth: NextAuth.js with JWT strategy + 
bcrypt
 password hashing
- AI: OpenAI gpt-4o (supports vision + text)
- RAG: 
pgvector
 or Pinecone for vector store; text-embedding-3-small for embeddings
- PDF: react-pdf/renderer for client-side PDF generation
- Real-time: WebSocket via socket.io for live violation streaming
- File storage: local /public/uploads/ or S3-compatible (env-configurable)
════════════════════════════════════════════════
FOLDER STRUCTURE
════════════════════════════════════════════════
/
app
  /
api
    /auth
/[
...
nextauth
]/
route.ts
    /violations/
route.ts
    /violations/[id]/
route.ts
    /chat/
route.ts
    /upload/
route.ts
    /report/[id]/
route.ts
    /analytics/
route.ts
  /(auth)
    /login/
page.tsx
    /register/
page.tsx
  /(dashboard)
    /
layout.tsx
           ← shared navbar + sidebar shell
    /
page.tsx
             ← main dashboard
    /ai/
page.tsx
          ← chatbot
    /history/
page.tsx
     ← analytics + violation history
    /settings/
page.tsx
    ← camera, zone, PPE thresholds, profile
/
components
  /
ui
/                    ← 
shadcn
 primitives
  /layout/
    
Navbar.tsx
    
Sidebar.tsx
    
NotificationPanel.tsx
  /dashboard/
    
UploadPanel.tsx
    
WebcamPanel.tsx
    
ViolationFeed.tsx
    
ViolationCard.tsx
    
ViolationModal.tsx
    
StatCard.tsx
  /ai/
    
ChatWindow.tsx
    
ChatMessage.tsx
    
ChatHistorySidebar.tsx
    
FileUploadButton.tsx
  /reports/
    
ViolationReport.tsx
   ← react-pdf document component
    
DownloadButton.tsx
  /analytics/
    
ViolationChart.tsx
    
TrendCard.tsx
/
lib
  
prisma.ts
  
auth.ts
  
openai.ts
  
rag.ts
  
websocket.ts
  
violations.ts
  
pdf.ts
/
hooks
  
useWebcam.ts
  
useViolations.ts
  
useNotifications.ts
  
useChat.ts
/
types
  
index.ts
               ← all shared TypeScript types
/
prisma
  
schema.prisma
/.
env
.local
              ← all secrets (never committed)
════════════════════════════════════════════════
DATABASE SCHEMA (Prisma)
════════════════════════════════════════════════
model User {
  id            String    @id @default(
cuid(
))
  name          String
  email         String    @unique
  
passwordHash
  String
  role          
Role
      @
default(
OPERATOR)
  
createdAt
     
DateTime
  @
default(
now(
))
  violations    
Violation[
]
  
chatSessions
  
ChatSession
[]
}
enum
 Role 
{ ADMIN
 OPERATOR 
VIEWER }
model Violation {
  id              String   @id @default(
cuid(
))
  
violationType
   String
  confidence      Float
  
riskLevel
       String   // CRITICAL | HIGH | MEDIUM | LOW
  
zoneId
          String
  
cameraId
        String
  
screenshotPath
  String
  description     String   @
db.Text
  
frameId
         Int
  detections      Json
  timestamp       
DateTime
 @default(
now(
))
  
userId
          String
  user            
User
     @
relation(
fields: [
userId
], references: [id])
}
model 
ChatSession
 {
  id        String        @id @default(
cuid(
))
  title     String
  
userId
    String
  user      
User
          @
relation(
fields: [
userId
], references: [id])
  
messages  
ChatMessage
[]
  
createdAt
 
DateTime
      @default(
now(
))
}
model 
ChatMessage
 {
  id          String      @id @default(
cuid(
))
  role        String      // user | assistant
  content     String      @
db.Text
  
fileUrl
     String?
  
sessionId
   String
  session     
ChatSession
 @
relation(
fields: [
sessionId
], references: [id])
  
createdAt
   
DateTime
    @default(
now(
))
}
════════════════════════════════════════════════
ENVIRONMENT VARIABLES 
(.
env
.local
)
════════════════════════════════════════════════
DATABASE_URL=mysql://user:pass@localhost:3306/visionrag
NEXTAUTH_SECRET=your-secret-here
NEXTAUTH_URL=http://localhost:3000
OPENAI_API_KEY=
sk
-...
PINECONE_API_KEY=...                  # or leave blank to use local 
pgvector
PINECONE_INDEX=
visionrag
-rag
NEXT_PUBLIC_SOCKET_URL=http://localhost:3001
UPLOAD_DIR
=./
public/uploads           # or S3 bucket name
MAX_UPLOAD_SIZE_MB=50
YOLO_API_URL=http://localhost:8000    # your Python YOLO backend
BEEP_SOUND_URL=/sounds/alert.mp3
════════════════════════════════════════════════
1. LOGIN / REGISTER PAGE
════════════════════════════════════════════════
Route: /login and /register (or a unified /(auth)/
page.tsx
 with tab toggle)
Left half (50% width, min-h-screen):
- Background: linear gradient from #0ea5e9 (sky-500) to #1d4ed8 (blue-700)
- 
Centered
 vertically with flexbox
- Large bold white text: "Compliance Vision AI"
- Subtitle: "COMPLIANCE DASHBOARD"
- Body text: "Real-time PPE violation detection, RAG-powered regulatory citation, 
  and automated compliance report generation."
- Decorative: animated floating cards or blurred circle shapes (Framer Motion) 
  to add depth — no images required
- Add the tagline: "Trusted by safety teams across construction, manufacturing, 
  and logistics."
- Hide left side below md breakpoint (mobile shows only the form)
Right half (50% width):
- White / 
bg
-white background
- 
Centered
 card with tab switcher: Login | Register
- Login fields: Email, Password
- Register fields: Full Name, Email, Password, Confirm Password
- All fields: rounded-xl, soft border, focus ring in sky-500
- Submit button: full width, bg-sky-600 hover:bg-sky-700, rounded-xl
- Validation: 
  - Email format check
  - Password min 8 chars
  - Confirm password match
  - Show inline error messages under each field (red, small)
- Loading state: spinner inside submit button while API call is pending
- Success: redirect to /dashboard with session cookie
- Error: red toast notification at top of form
- "Forgot password?" link (placeholder route /forgot-password)
- OAuth optional: add "Continue with Google" button if 
NextAuth
 Google provider added
- Fully responsive: stacks vertically on mobile, left brand panel hides
════════════════════════════════════════════════
2. NAVBAR
════════════════════════════════════════════════
Component: /components/layout/
Navbar.tsx
Sticky, z-50, backdrop-blur, border-b border-sky-100
Left side:
- Logo icon (shield or camera icon from 
lucide
-react) + "Compliance Vision AI" in 
  font-bold text-sky-700
Center
 (hidden on mobile):
- Nav links: Dashboard | AI | History | Settings
- Active link underlined with sky-500
Right side:
- "Welcome, {user.name}" — truncate at 20 chars
- Notification bell icon (
lucide
 
BellRing
)
  - Red badge showing unread count
  - On click: slide-down 
NotificationPanel
 (fixed panel, not modal)
  - 
NotificationPanel
 shows last 20 alerts with timestamp, violation type, 
    "View" and "Download PDF" actions per alert
  - Bell shakes with CSS keyframe animation when new violation arrives
- User avatar circle (initials) with dropdown: Profile | Settings | Logout
- "AI" pill button (sky gradient) that routes to /ai
Mobile: hamburger menu with slide-out drawer
════════════════════════════════════════════════
3. DASHBOARD LAYOUT
════════════════════════════════════════════════
Route: /dashboard (protected, redirect to /login if no session)
Two-column layout:
- Left: 40% — 
UploadPanel
- Right: 60% — 
MonitoringPanel
- On tablet (md): stack vertically, left on top
- On mobile: single column
════════════════════════════════════════════════
4. LEFT PANEL — UPLOAD & DETECTION
════════════════════════════════════════════════
Component: 
UploadPanel.tsx
Tab selector at top: [ 
Image ]
 [ 
Video ]
 [ Live 
Webcam ]
Animated tab indicator with Framer Motion 
layoutId
── IMAGE MODE ──
- Drag-and-drop zone: dashed border, sky-100 
bg
, upload icon, 
  "Drag image here or click to browse"
- Accepted types: .jpg .jpeg .
png
 .
webp
- Max size: 10 MB (show error if exceeded)
- On file selected: show thumbnail preview (object-fit cover, rounded-xl)
- Show file name and size below preview
- "Analyze Image" button → POST /
api
/upload with 
FormData
 → 
  forward to YOLO_API_URL → receive detections → run 
ViolationLogicEngine
 → 
  store violation → emit socket event → update dashboard
- Show detection status badge: "
Analyzing
..." | "Done — N violations found" | "Error"
- Below preview: "Detected Violations" section showing inline violation chips 
  (
colored
 by risk level)
── VIDEO MODE ──
- Same drag-and-drop zone, accepts .mp4 .avi .mov, max 50 MB
- On file selected: show <video> preview element with controls
- "Run Analysis" button appears after file is selected
- Progress bar during processing (simulate or use real SSE stream from backend)
- Show violations as they are found, appended to a scrollable list below the video
- Each found violation: thumbnail + type + confidence chip
── LIVE WEBCAM MODE ──
- Request camera permission via 
getUserMedia
- Show live <video> stream in a rounded card with a pulsing red "LIVE" badge
- Frame sampling: capture a frame every 2 seconds → POST to /
api
/upload 
  (base64 encoded) → YOLO → violations → emit socket
- No "Run Analysis" button — detection is automatic and continuous
- Show "Detection active" status with animated pulse dot
- "Stop" button to halt sampling
- Show last detected violation inline below the stream
── COMMON ──
- Screenshot gallery: grid of last 6 violation screenshots from current session
- Each screenshot is clickable → opens 
ViolationModal
════════════════════════════════════════════════
5. RIGHT PANEL — MONITORING & VIOLATIONS
════════════════════════════════════════════════
Component: 
MonitoringPanel.tsx
── STAT CARDS (top row, 4 cards) ──
Each card: white 
bg
, rounded-xl, shadow-
sm
, sky-50 border
- Total Violations: count, sky icon, animated count-up on load
- Critical: count with red badge
- High Risk: count with orange badge
- Violation Types: count of unique 
violation_type
 values
Clicking any card filters the violation feed below
── VIOLATION FEED ──
- Scrollable grid (2 columns on large, 1 on mobile)
- Each 
ViolationCard
:
  - Screenshot thumbnail (aspect-video, object-cover, rounded-
lg
)
  - Violation type label (bold, 
colored
)
  - Confidence: progress bar + percentage
  - Risk level badge: CRITICAL=red HIGH=orange MEDIUM=yellow LOW=green
  - Camera ID and Zone ID (muted small text)
  - Timestamp (relative: "2 min ago")
  - "View Details" button → opens 
ViolationModal
  - "Download PDF" button → triggers PDF generation and download
- New cards animate in from top with Framer Motion (initial opacity 0, y -20)
- Real-time: new violations pushed via WebSocket and prepended to feed
── VIOLATION MODAL ──
Component: 
ViolationModal.tsx
- Full-screen overlay or large 
centered
 modal
- Large screenshot with 
bbox
 annotation overlay (draw rectangles using canvas 
  over the screenshot based on detection coordinates)
- Full violation details in a clean two-column layout
- Timeline: when detected, by which camera, in which zone
- "Generate Full Report" → PDF download
- "Mark as Reviewed" button → PATCH /
api
/violations/[id]
- "Dismiss" / close
════════════════════════════════════════════════
6. NOTIFICATIONS & ALERTS
════════════════════════════════════════════════
Component: 
NotificationPanel.tsx
 + 
useNotifications.ts
 hook
- WebSocket listener in 
useNotifications
 hook receives violation events
- On new violation:
  1. Play beep: new Audio(
process.env.NEXT
_PUBLIC_BEEP_URL
).play
()
  2. Show toast (bottom-right, Framer Motion slide-in, auto-dismiss 6s)
     Toast shows: violation type + risk badge + camera ID + "View" button
  3. Increment bell badge count
  4. Prepend to notification list in 
NotificationPanel
- 
NotificationPanel
 (slide-down from navbar):
  - Header: "Alerts" + "Mark all as read" button
  - List of last 50 notifications, grouped by date
  - Each row: 
colored
 dot + violation type + zone + timestamp + "View" + "PDF"
  - Unread rows have sky-50 background
  - Empty state: "No alerts yet" with bell illustration
- Notifications persist across page refreshes (stored in DB and fetched on load)
════════════════════════════════════════════════
7. PDF REPORT GENERATION
════════════════════════════════════════════════
Component: /components/reports/
ViolationReport.tsx
 (react-pdf Document)
Trigger: "Download PDF" button on any 
ViolationCard
 or modal
PDF layout (A4, portrait):
HEADER:
- Left: Shield icon + "Compliance Vision AI | Compliance Report"
- Right: Report ID + Generated: {date}
- Horizontal rule below header
SECTION 1 — Incident Summary
- Violation Type (large, 
colored
)
- Detection Timestamp
- Camera ID / Zone ID
- Confidence Score (shown as percentage bar)
- Risk Level (
colored
 badge)
- Frame ID
SECTION 2 — Evidence
- Screenshot image (full width, captioned)
- Detection overlay description (list of detected objects + 
bboxes
)
SECTION 3 — AI Recommendation (optional, from OpenAI)
- Short 2-3 sentence recommendation for corrective action
- Regulatory reference (from RAG knowledge base)
FOOTER:
- "This report was generated automatically by 
Compliance Vision AI
 AI."
- Page number
Download triggers browser file download as:
"
ViolationReport
_{
violationType
}_{timestamp}.pdf"
════════════════════════════════════════════════
8. AI PAGE
════════════════════════════════════════════════
Route: /ai (protected)
Layout: two-column
- Left sidebar (280px): Chat history list
- Right main area: active chat window
── LEFT SIDEBAR ──
- "New Chat" button at top (+ icon)
- List of past 
ChatSessions
: title (auto-generated from first message), date
- Active session highlighted
- Hover: show delete icon
- Sessions fetched from GET /
api
/chat/sessions
- Clicking session loads its messages into the main window
── MAIN CHAT WINDOW ──
Greeting at top (fades out once chat starts):
  "Hello, {user.name} 
👋
  How
 can I help you today?"
Message thread:
- User messages: right-aligned, sky-600 
bg
, white text, rounded-xl
- Assistant messages: left-aligned, white 
bg
, border, rounded-xl
- Each message: avatar (initials circle or bot icon) + content + timestamp
- Code blocks rendered with syntax highlighting (react-syntax-highlighter)
- Markdown rendered (react-markdown)
- Typing indicator: three animated dots when assistant is responding (streaming)
Input bar (pinned to bottom):
- 
Textarea
 (auto-resize, max 4 rows) — "Ask anything about safety compliance..."
- File upload button (paperclip icon): accepts .jpg .
png
 .pdf
  - On attach: show file chip above input with remove button
  - File is base64-encoded and sent with the message
- Send button (sky-600, arrow icon)
- "Clear" button to start new session
API call to POST /
api
/chat:
- Send: 
{ 
sessionId
, messages (full history), fileBase64 (optional
) }
- Stream response using OpenAI streaming API
- System prompt includes:
  "You are 
Compliance Vision AI
 AI's safety compliance assistant. You help users understand 
   PPE violation reports, safety regulations, corrective actions, and best practices.
   You are grounded in the uploaded compliance knowledge base. Be professional, 
   clear, and concise."
- If file attached: use gpt-4o vision to analyze image or extract PDF text first, 
  then respond
- RAG retrieval: embed user query → search vector store → inject top-3 relevant 
  chunks into system prompt context before sending to OpenAI
Response streaming: use 
ReadableStream
 / Server-Sent Events from the API route,
consume with 
EventSource
 or fetch + 
ReadableStream
 in the client.
Store each message in DB (
ChatMessage
) after stream completes.
════════════════════════════════════════════════
9. HISTORY / ANALYTICS PAGE
════════════════════════════════════════════════
Route: /history
── TOP FILTERS ──
- Date range picker (start date, end date)
- Violation type multi-select dropdown
- Risk level filter chips
- Camera ID filter
── CHARTS (Recharts) ──
Row 1:
- Line chart: Violations over time (last 30 days, daily)
- Donut chart: Violations by type (PPE_HELMET_MISSING etc.)
Row 2:
- Bar chart: Violations by camera ID
- Bar chart: Violations by zone
── VIOLATION TABLE ──
- Paginated table (20 per page)
- Columns: Timestamp | Type | Risk | Camera | Zone | Confidence | Actions
- Sortable columns
- "Download PDF" action per row
- "Export CSV" button at top right for bulk export
════════════════════════════════════════════════
10. SETTINGS PAGE
════════════════════════════════════════════════
Route: /settings (Admin only; Operator sees only Profile tab)
Tabs: Profile | Camera Config | Zone Rules | PPE Thresholds | Team
── PROFILE ──
- Edit name, email, change password form
── CAMERA CONFIG ──
- Table of cameras (id, label, stream URL)
- Add / Edit / Delete camera rows
── ZONE RULES ──
- For each zone: toggle 
requires_hardhat
, 
requires_vest
, 
requires_mask
, 
  
requires_goggles
, 
requires_gloves
, restricted, 
max_occupancy
- Saved to DB and consumed by 
ViolationLogicEngine
── PPE THRESHOLDS ──
- Confidence threshold slider per violation type
- Min confidence to log a violation (default 0.6)
── TEAM ──
- Table of users with roles
- Invite user form (email + role)
- Change role dropdown
- Remove user
════════════════════════════════════════════════
11. ROLE-BASED ACCESS CONTROL
════════════════════════════════════════════════
Roles: ADMIN | OPERATOR | VIEWER
Middleware (/
middleware.ts
):
- Redirect unauthenticated users to /login
- Redirect VIEWER away from /settings
- Redirect OPERATOR away from /settings/team
In 
NextAuth
 session, include role.
Use 
useSession
(
) + role check in protected components.
Show/hide action buttons based on role (e.g. VIEWER cannot mark as reviewed).
════════════════════════════════════════════════
12. WEBSOCKET (REAL-TIME)
════════════════════════════════════════════════
Server: socket.io server at NEXT_PUBLIC_SOCKET_URL (can be same Next.js server 
via custom 
server.ts
, or separate Node.js process)
Events:
- Server → Client: "
violation:new
" 
{ violation
 } — new violation detected
- Server → Client: "
violation:update
" 
{ id
, 
status }
 — violation status changed
- Client → Server: "
join:camera
" 
{ 
cameraId
 } — subscribe to camera feed
Client hook: 
useViolations.ts
- Connects on dashboard mount, disconnects on unmount
- On "
violation:new
": update violations state, trigger notification
════════════════════════════════════════════════
13. DESIGN SYSTEM
════════════════════════════════════════════════
Color
 palette (Tailwind config extend):
- Primary: sky-500 (#0ea5e9), sky-600 (#0284c7), sky-700 (#0369a1)
- Surface: white, sky-50 (#f0f9ff)
- Text: slate-900 (primary), slate-500 (muted)
- Risk levels:
  CRITICAL: red-600 bg-red-50
  HIGH: orange-500 bg-orange-50
  MEDIUM: yellow-500 bg-yellow-50
  LOW: green-500 bg-green-50
Typography:
- Font: Inter (Google Fonts) or Geist (Next.js default)
- Headings: font-semibold, tracking-tight
- Body: font-normal, leading-relaxed
Component rules:
- All cards: rounded-2xl shadow-
sm
 border border-sky-100
- All inputs: rounded-xl border-slate-200 
focus:border
-sky-500 
focus:ring
-2 
  
focus:ring
-sky-100
- All primary buttons: bg-sky-600 hover:bg-sky-700 text-white rounded-xl 
  transition-all
- 
Glassmorphism
 panels: 
bg
-white/70 backdrop-blur-md border border-white/50
- Page container: max-w-screen-2xl mx-auto px-6
Animations (Framer Motion):
- Page transitions: opacity 0→1, y 10→0, duration 0.3
- Cards: stagger children with 0.05s delay
- New violation card: slide-in from top
- Modal: scale 0.95→1 + fade
- Sidebar: x -280→0
Dark mode:
- Implement via next-themes
- Toggle in navbar (sun/moon icon)
- All Tailwind classes use dark: variants
Responsive breakpoints:
- Mobile <640px: single column, hidden sidebar
- Tablet 640–1024px: stacked dashboard panels
- Desktop >1024px: full two-column layout
════════════════════════════════════════════════
14. ERROR HANDLING & LOADING STATES
════════════════════════════════════════════════
- Every async action has a loading spinner state
- Skeleton loaders (animated 
gray
 pulses) for:
  - Violation feed while fetching
  - Stat cards
  - Chat history
  - Analytics charts
- Error boundary component wrapping each major section
  (shows "Something went wrong" card with retry button)
- API errors: toast notification with error message
- File upload errors: inline error below 
dropzone
- Empty states: illustrated empty-state card for:
  - No violations yet
  - No chat sessions
  - No analytics data in range
════════════════════════════════════════════════
15. SECURITY & PRODUCTION RULES
════════════════════════════════════════════════
- Never hardcode secrets — all 
in .
env
.local
- Rate-limit all API routes (use 
upstash
/
ratelimit
 or next-rate-limit)
- Sanitize file uploads: check MIME type, scan for executable extensions
- File size limits enforced server-side (not just client-side)
- CSRF protection via 
NextAuth
 built-in
- SQL injection: impossible via Prisma ORM parameterized queries
- XSS: sanitize any user-generated content before rendering 
  (
DOMPurify
 or server-side escape)
- Auth token expiry: 24h JWT, refresh on activity
- Violation screenshots stored outside /public if sensitive 
  (serve via signed URL or /
api
/file/[id] route with auth check)
- Input validation: use 
zod
 schemas on all API routes
════════════════════════════════════════════════
16. CODE QUALITY RULES
════════════════════════════════════════════════
- TypeScript strict mode (
tsconfig
: "strict": true)
- All props typed with interfaces in /types/
index.ts
- No any types — use proper generics
- Reusable components for: 
StatCard
, 
ViolationCard
, 
RiskBadge
, 
  
UploadZone
, 
ChatBubble
, 
SkeletonCard
, 
EmptyState
, 
ErrorBoundary
- Custom hooks for all stateful logic (no logic in page components)
- API routes use try/catch with consistent error response shape:
  
{ success
: 
boolean
, 
data?:
 T, 
error?:
 
string }
- Consistent date formatting with date-
fns
- All strings: no hardcoded — use a 
constants
.ts
 file
- Prettier + 
ESLint
 configured
════════════════════════════════════════════════
DELIVERABLE
════════════════════════════════════════════════
Generate:
1. Complete folder structure listing
2. 
prisma
/
schema.prisma
3. All page files (
page.tsx
 for each route)
4. All component files listed above
5. All API route handlers
6. All custom hooks
7. /types/
index.ts
8. /lib/*.
ts
 utility files
9. 
tailwind.config.ts
 with custom 
colors
10. .
env
.local.example
11. README.md with setup instructions
Prioritize: Login page → Navbar → Dashboard (upload + monitoring panels) → 
Notification system → PDF report → AI chat page → History → Settings.
Make every component production-ready, typed, and immediately runnable 
with `
npm
 run dev` after environment setup.